<Window x:Class="BootEase.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BootEase"
        mc:Ignorable="d"
        Title="BootEase" Height="300" Width="400"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004578"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Language Button Container -->
        <Grid Grid.Row="0">
             <Button x:Name="LangButton" Content="عربي" HorizontalAlignment="Right" VerticalAlignment="Top" 
                Margin="0,-10,-10,0" Padding="8,4" Background="Transparent" BorderThickness="0" Foreground="#0078D7"
                Cursor="Hand" Click="LangButton_Click" FontSize="12" FontWeight="SemiBold"/>
        </Grid>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,10,0,20">
            <TextBlock x:Name="AppTitleText" Text="BootEase" FontSize="24" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Center"/>
            <TextBlock x:Name="SubtitleText" Text="Simple BIOS/UEFI Rebooter" FontSize="14" Foreground="#666" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock x:Name="StatusText" Text="Checking system status..." FontSize="14" Foreground="#333" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,20"/>
            
            <Button x:Name="RebootButton" Content="Restart &amp; Enter BIOS" Style="{StaticResource ModernButtonStyle}" 
                    Click="RebootButton_Click" MinWidth="200" Width="Auto" Padding="20,0" Height="45"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="12">
                 <Hyperlink NavigateUri="https://github.com/voidksa/BootEase" RequestNavigate="Hyperlink_RequestNavigate">
                     GitHub
                 </Hyperlink>
            </TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock x:Name="UpdateText" Visibility="Collapsed" Margin="0,0,10,0" FontSize="11">
                    <Hyperlink NavigateUri="https://github.com/voidksa/BootEase/releases/latest" RequestNavigate="Hyperlink_RequestNavigate" Foreground="Red" FontWeight="Bold">
                        <Run x:Name="UpdateRun" Text="Update Available!"/>
                    </Hyperlink>
                </TextBlock>
                <TextBlock x:Name="VersionText" Text="v1.0.0" FontSize="10" Foreground="#999"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
